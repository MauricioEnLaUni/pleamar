/*import log from "../../../api/log.js";
import LogsConnection from "../LogsConnection.js";

export default (request: any) => {
    const connection = new LogsConnection();
    return log(request, connection);
};
*/